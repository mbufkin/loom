# Fixtures (not operator corpora)

Tiny ingest smoke datasets. **Do not** treat these as district curricula.

| Fixture | Purpose |
|---------|---------|
| `ingest-pilot/` | Minimal ingest shape |
| `ingest-test/` | Broader ingest test manifest |

Operator datasets live as siblings under `projects/` (see [STATUS.md](../STATUS.md)).
