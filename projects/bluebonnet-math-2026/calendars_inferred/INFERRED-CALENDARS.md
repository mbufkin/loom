# Inferred calendars — `bluebonnet-math-2026`

**source:** `inferred_from_documents`  
**generated:** 2026-07-17T20:56:31.008580+00:00

Early `rollup.py` / `pacing-plan.yaml` is provisional. This file is the post-assemble evidence calendar.

## Year note

The data shows that both the Grade 5 math module and its accompanying program guide have no recorded evidence of lessons on any days, leaving all scheduled days empty. This suggests a complete lack of curriculum pacing information for these resources across the observed period. Consequently, it is impossible to determine whether units are under‑developed or fully stocked based solely on this dataset. Additional evidence would be required to assess instructional coverage and pacing accuracy.

## Grade 5 Math — Module 1 (`g5-mod-1`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — program guides (`g5-program`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
