# Inferred calendars — `bluebonnet-math-2026`

**source:** `inferred_from_documents`  
**generated:** 2026-07-20T02:10:21.509155+00:00

Early `rollup.py` / `pacing-plan.yaml` is provisional. This file is the post-assemble evidence calendar.

## Year note

The curriculum shows no evidence of actual instruction across any Grade 5 math module or the overarching program guide—each entry reports zero days with documented activity and a full complement of empty‑day placeholders (13 for each module, five for the program). Consequently, all modules appear completely underpopulated relative to their intended pacing. This suggests that either no lessons have been delivered yet in this reporting period or the documentation system is missing entries. As it stands, there are no units demonstrating sufficient content coverage; every unit is marked as thinly stocked with only placeholder days remaining.

## Grade 5 Math — Module 1 (`g5-mod-1`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — Module 2 (`g5-mod-2`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — Module 3 (`g5-mod-3`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — Module 4 (`g5-mod-4`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — Module 5 (`g5-mod-5`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — Module 6 (`g5-mod-6`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
- `d6`: **EMPTY** — —
- `d7`: **EMPTY** — —
- `d8`: **EMPTY** — —
- `d9`: **EMPTY** — —
- `d10`: **EMPTY** — —
- `d11`: **EMPTY** — —
- `d12`: **EMPTY** — —
- `d13`: **EMPTY** — —

## Grade 5 Math — program guides (`g5-program`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —
