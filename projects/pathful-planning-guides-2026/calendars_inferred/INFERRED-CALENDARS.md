# Inferred calendars — `pathful-planning-guides-2026`

**source:** `inferred_from_documents`  
**generated:** 2026-07-17T16:12:20.475900+00:00

Early `rollup.py` / `pacing-plan.yaml` is provisional. This file is the post-assemble evidence calendar.

## Year note

The planning guides show no evidence of actual lesson content or pacing details across all units. Empty days range from 1 to 4 per guide, indicating significant gaps in documented instructional sequences. No specific unit appears stocked with material while others are thin; the absence is consistent and systemic throughout every document provided. All titles lack any referenced curriculum structure, standards alignment, or activity sequencing evidence.

## Activity Sequence Flowchart Planning Guide (`activity-sequence-flowchart`)

- `d1`: **EMPTY** — —

## Planning Guide CCE High School (`cce-high-school`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —

## Planning Guide CCE Middle School (`cce-middle-school`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —

## PreETS Roadmap Planning Guide (`preets-roadmap`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —

## Special Education Transition Planning Guide (`special-education-transition`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —

## Planning Guide WBL High School (`wbl-high-school`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —

## Planning Guide WBL Middle School (`wbl-middle-school`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
