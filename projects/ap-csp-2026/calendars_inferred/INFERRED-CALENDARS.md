# Inferred calendars — `ap-csp-2026`

**source:** `inferred_from_documents`  
**generated:** 2026-07-22T16:50:41.648366+00:00

Early `rollup.py` / `pacing-plan.yaml` is provisional. This file is the post-assemble evidence calendar.

## Year note

The curriculum shows significant gaps in coverage across Big Ideas 2, 3 and 4, each with zero days of documented evidence despite having empty slots ranging from three to five. Only one day is recorded for the Impact‑of‑Computing unit (Big Idea 5), which also has a few empty spaces left. In contrast, Creative Development (Big Idea 1) already occupies all four allocated slots and shows no gaps. These patterns suggest that units requiring more instructional time are under‑represented in the current pacing plan.

## Algorithms and Programming (Big Idea 3) (`algorithms-programming`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —
- `d5`: **EMPTY** — —

## Creative Development (Big Idea 1) (`creative-development`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —

## Data (Big Idea 2) (`data`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —

## Impact of Computing (Big Idea 5) (`impact-computing`)

- `d1`: **HAS_EVIDENCE** — lesson_content
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
- `d4`: **EMPTY** — —

## Computer Systems and Networks (Big Idea 4) (`systems-networks`)

- `d1`: **EMPTY** — —
- `d2`: **EMPTY** — —
- `d3`: **EMPTY** — —
