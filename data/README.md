# data/ — Backup & OSINT (Zone B)

Raw curriculum copies and research backups. **Not** pipeline I/O.

| Path | Purpose |
|------|---------|
| `career-curriculum/osint/` | OSINT corpus backup (prefer `projects/<id>/sources/` for audits) |

**MUST NOT** treat this tree as deliverables. Operator outputs live under `projects/<id>/output/`.  
Legacy crystallize batch output was moved to `archive/career-curriculum-output/`.

See [PROJECT_STRUCTURE.md](../PROJECT_STRUCTURE.md) (canonical layout).
