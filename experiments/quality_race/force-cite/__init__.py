# force-cite quality-race experiment package.
# Additive-only: nothing here mutates the shared lesson-scoring stack; it only
# imports and reuses helpers from it (lesson_scoring, lesson_scorers, audit_lib).
