# Experiments — archived off-repo

These runners were moved out of the workspace so they are not runnable from Loom.

**Archive location:** `/home/lenovo/archive/loom-experiments-20260803T141549Z/experiments`

Canonical path: **one program** `./run-audit` →
`projects/<curriculum>/e2e/runs/<model>/` for review (see `docs/E2E.md`).

Do not recreate spike/experiment harnesses here.
