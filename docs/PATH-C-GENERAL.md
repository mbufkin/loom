# Path C — General / unknown workflow (stub)

Entry: Loom router typed the document as `general` (slides, worksheet, rubric, unknown, …).

## Steps (placeholders)

| Step | Name | Intent |
|------|------|--------|
| **C1** | Generic inventory | List chunks / roles present |
| **C2** | Generic checks | Light presence checks — TBD |
| **C3** | Feedback log | Append `_loom_feedback.yaml` when type is weak/unknown |

## Guardrails

- Always a valid fallback — never block the pipeline.
- Feedback is data you read later to grow new paths (Path D…).
- Auditor-only.

## Outputs (stub)

- `path_c/findings.json`
- Project-level `_loom_feedback.yaml` entries for unknown patterns
