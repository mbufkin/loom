# Post-Reorganization Path Changes (2026-07-07)

Use this when updating **repo docs**, operator notes, Roam/Notion, or partner runbooks after Phase 1 cleanup.

---

## What changed (operator-facing)

| Before | After |
|--------|-------|
| `career-curriculum/osint/` | `data/career-curriculum/osint/` |
| `legacy/README.md` | `archive/crystallize-legacy/README.md` |
| `crystallize.py` (root) | `archive/crystallize-legacy/crystallize.py` |
| Run logs in `output/run-*.log` | `projects/dallas-career-2026/runs/run-*.log` |
| 50+ files at repo root | **Zone layout** — see [PROJECT_STRUCTURE.md](../PROJECT_STRUCTURE.md) |

**Unchanged (do not update these):**

- `./run-audit dallas-career-2026`
- `projects/<id>/output/` — all deliverables still here
- `config.yaml` model URLs (8080 / 30000)

---

## Commands (copy-paste)

```bash
cd ~/g10-control-center

# Full Dallas re-audit (Option A)
./run-audit dallas-career-2026 --force

# Smoke one unit
./run-audit dallas-career-2026 --only engineering --force

# Tests
python3 test_audit.py
python3 test_doc_extract.py
python3 test_rollup.py
python3 test_schema_validate.py
```

Legacy corpus path in config:

```yaml
paths:
  default_sources_dir: "data/career-curriculum/osint"
```

Dallas manifest uses project-local sources (preferred):

```yaml
sources_dir: sources   # → projects/dallas-career-2026/sources/
```

---

## Where things moved

| Zone | Path | Purpose |
|------|------|---------|
| Product | repo root `*.py`, `run-audit` | Pipeline |
| Data | `data/career-curriculum/` | Raw OSINT corpus |
| Experiments | `experiments/openscied/`, `experiments/apcsp/` | Not in `run_project.py` |
| Tools | `tools/` | Debug scripts only |
| Archive | `archive/crystallize-legacy/` | Old 4-layer batch |
| Archive | `archive/career-curriculum-output/` | Frozen legacy batch outputs (was `data/career-curriculum/output/`) |
| Archive | `archive/reviews/` | G10 forensic reviews |
| Archive | `archive/ornith/` | Unrelated dashboard experiment |

---

## Roam / external wiki bullets

Paste into your Roam graph or partner wiki:

- Crystallize repo reorganized 2026-07-07; layout SoT = `PROJECT_STRUCTURE.md`
- Dallas golden project: `projects/dallas-career-2026/`
- Deliverables: `output/GLOBAL-AUDIT-REPORT.pdf` + 18 unit PDFs
- Run logs: `runs/` not mixed with `output/`
- Corpus backup: `data/career-curriculum/osint/` (111 files)
- Full test command: `./run-audit dallas-career-2026 --force`
- Stable acceptance: exit 0, 18 unit PDFs, SUMMARY.md 8 OK / 10 GAPS (approx)

---

## Still TODO (not reorg)

- [ ] Commit Phase 1 layout to git
- [ ] Phase 2: optional `pipeline/` subfolder
- [ ] Corrected calendar artifact after Tier A placement
- [ ] GitHub repo sync

---

## Verification after any doc update

1. `python3 test_audit.py` — all pass  
2. `./run-audit dallas-career-2026 --only engineering --force` — exit 0  
3. Open `projects/dallas-career-2026/output/GLOBAL-AUDIT-REPORT.pdf`
